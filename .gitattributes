# Shell scripts must keep LF endings. A .sh file checked out with CRLF fails on
# Linux with a confusing "bad interpreter: /bin/bash^M" error, which is a very
# easy way to lose an hour on the cluster.
*.sh text eol=lf
